import { createTheme } from "@mui/material/styles"

const mui = createTheme({
  palette: {
    primary: {
      // light: "#",
      main: "#FE024E",
      // dark: "#",
      contrastText: "#fff",
    },
    secondary: {
      // light: "#",
      main: "#EB507F",
      // dark: "#",
      contrastText: "#000",
    },
  },
})

export default mui
