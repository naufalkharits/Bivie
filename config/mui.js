import { createTheme } from "@mui/material/styles"

const mui = createTheme({
  palette: {
    primary: {
      main: "#FE024E",
      contrastText: "#fff",
    },
    secondary: {
      main: "#EB507F",
      contrastText: "#fff",
    },
  },
})

export default mui
